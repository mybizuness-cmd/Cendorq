export default function ProfileLoadingPage() {
  return (
    <main className="relative mx-auto max-w-7xl overflow-hidden px-4 py-12 text-white sm:px-6 md:py-16 xl:py-20">
      <section className="system-panel-authority rounded-[2rem] p-8 text-center">
        <div className="system-chip inline-flex rounded-full px-3 py-1.5 text-[11px] font-semibold uppercase tracking-[0.22em] text-cyan-200">Profile</div>
        <h1 className="mt-5 text-4xl font-semibold tracking-tight text-white sm:text-5xl">Preparing the Cendorq profile view.</h1>
        <p className="mx-auto mt-6 max-w-2xl text-base leading-8 text-slate-300">The route is resolving the platform doctrine, system posture, and sequence map before the final view renders.</p>
      </section>
    </main>
  );
}
